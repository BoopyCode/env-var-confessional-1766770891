# ENV Var Confessional

A simple tool that interrogates your environment variables before they cause production drama. Run it to catch missing or misconfigured variables before they ruin your day.

Usage: `node env-confessional.js`